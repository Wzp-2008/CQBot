from ..pojo import *


class CommandExecutor(object):
    """
    just a template class of commandExecutor
    """
    def execute(self, runs: Message) -> bool:
        """
        when the command activated to call this function
        :param runs:the message
        :return:is this command runs well?
        """
        pass
